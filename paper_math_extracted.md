001: XXXX题目
002: 摘要：xxx
003: 关键词：xxx
004: 第一部分：线图转换建模
005: 对拓扑结构与方向演化构建对应的有向线图
006: 设原始有向加权网络为$G=(V,E,W)$，其中$V$为节点集合，$E$为有向边集合，$W$为边权重集合。为保留原网络中的信息流向与因果传递逻辑，构建对应的有向线图$L(G)=(V^{L},E^{L},W^{L})$，其拓扑方向映射规则定义如下：
007: 元素身份转换：原图$G$中的任意一条有向边$e_{i}=(u,v)∈E$，在映射后转化为线图$L(G)$中的一个独立节点$v_{i}^{L}∈V^{L}$。即原图的边规模决定了线图的节点规模：$|V^{L}|=|E|$。
008: 有向连边逻辑：线图$L(G)$中的有向边集合$E^{L}$反映了原图边的首尾邻接关系。当且仅当原图中边$e_{i}$的终点节点$v$同时也是边$e_{j}=(v,w)∈E$的起点节点时（即存在路径$u→v→w$），在线图中建立从节点$v_{i}^{L}$指向节点$v_{j}^{L}$的有向边，记为$e_{ij}^{L}=(v_{i}^{L},v_{j}^{L})∈E^{L}$。
009: 多维特征的权重映射机制
010: 在加权网络中，不仅需要转换网络拓扑，还需精确刻画线图中新生成连边$e_{ij}^{L}$的权重$W_{ij}^{L}$。该权重表征了信息或流量从原图边$e_{i}$转移至边$e_{j}$的连续渗透能力。针对不同的复杂系统动力学特征，本文提出三种典型的权重映射算子：
011: 传播概率算子（乘法映射）：适用于信息依概率衰减的传播网络，这种算子的核心物理意义在于计算独立事件连续发生的联合概率。在线图转换中，原图的相邻边$i$和$j$会转换为线图中的两个相邻节点，它们之间的连边权重$W_{ij}^{L}$代表了某种流动从边$i$顺利传递到边$j$的整体透过率。当原网络中的边权重代表通过率、留存率或衰减系数等取值在$[0,1]$区间内的概率属性时，信息或物质在经过多跳传输时的损耗是呈指数级累乘的，因此这种映射能够准确捕捉网络拓扑中衰减效应的真实情况。如在金融风控网络中，用于模拟风险传染与级联失效过程；在复杂网络与社交网络分析中，用于节点影响力评估与骨干链路预测；在通信与计算机网络架构中，用于系统可靠性与安全通道连通性分析。假设边权重代表通过率，则信息连续通过两边的联合概率为：
012: $W_{ij}^{L}=w_{i}×w_{j}#(1-1)$
013: 资源瓶颈算子（调和映射）：资源瓶颈算子即调和映射主要适用于交通运输、现代物流配送以及各类物理基础设施网络等实体流动受限的领域。这种算子的核心行为在于刻画资源传输过程中的木桶效应或短板效应，因为调和平均数在数学性质上会强烈偏向于两个数值中较小的一方。在实际应用中如果原网络连边权重代表道路的通行能力、管道的最大流量或供应链节点的处理容量，流体或货物在连续经过由边$w_{i}$和边$w_{j}$组成的路径时，其实际整体吞吐量往往受制于那段最狭窄或最拥堵的链路。通过使用调和映射计算$W_{ij}^{L}$，可以在线图转换中精准刻画这种容量限制，进而帮助识别出那些制约全局网络传输效率的关键瓶颈位置，为网络扩容或拥塞控制提供真实的拓扑量化依据。线图连边权重定义为原边权重的调和平均值：
014: $W_{ij}^{L}=((2·w_{i}·w_{j})/(w_{i}+w_{j}))#(1-2)$
015: 平滑均衡算子（几何映射）：平滑均衡算子即几何映射则更多应用于社会关系网络、金融交易网络以及生物相互作用网络等连接强度跨度极大且容易出现极端值的领域。该算子的关键行为是对局部极端权重进行非线性的平滑与均衡降噪，防止个别异常巨大的连接强度掩盖了网络真实的拓扑结构特征。在衡量网络结构洞或节点间综合交互强度时，如果相邻两条边的权重差异悬殊，使用普通的线性相加可能会过度放大强连接的影响，而几何平均数能够适度拉低极高值并保留连接的综合比例关系。这种映射行为有助于在保留链路连通性物理意义的同时，有效过滤掉原始权重数据中的极端噪声干扰，从而在全局视角下更客观地评估网络中不同社群之间的真实耦合程度或信息的稳定交互潜力。为降低局部极端权重对全局结构洞测度的干扰，采用几何平均平滑相邻边交互强度：
016: $W_{ij}^{L}=sqrt(w_{i}·w_{j})#(1-3)$
017: 通过上述算子，原图中边的多属性特征被成功转化为线图节点间的转移能力度量，满足了构建“转移能力矩阵”和“多阶邻域贡献度”的数据条件。
018: 动态时序网络的层间耦合映射
019: 针对网络结构随时间演化的特征，本研究将静态线图转换拓展至时序多层网络（TemporalMultiplexNetworks）。设动态时序网络由一系列时间切片构成，记为$G_{T}={G_{1},G_{2},…,G_{t}}$，其时序映射包含层内转换与层间耦合两个维度：
020: 层内时空切片转换：
021: 弧有向加权时序网络层内时空切片转换的理论基础在于将连续的动态演化过程离散化为一系列静态拓扑的有序集合。对时间窗口$t$内的网络切片$G_{t}$独立应用上述1.1与1.2节的规则，生成对应时刻的时序线图序列$L(G)_{T}={L(G_{1}),L(G_{2}),…,L(G_{t})}$。设定观察期窗口的起始时间$η$与总跨度$S$将整个演化周期$[η,η+S]$划分为$T$个时间层。依照时间窗口大小$τ=S/T$的等距划分原则构建出连续的时间区间序列${[η,η+τ],[η+τ,η+2τ],…,[η+(T-1)τ,η+S]}$。这种离散化处理使得原本复杂的时序网络被合理切割为$T$个离散且有序的时间层网络序列$G_{1},G_{2},…,G_{T}$。在每一个被切分出的时间层网络$G=(V,E)$中完整保留了该时间段内由所有节点构成的集合$V={v_{1},v_{2},…,v_{n}}$以及由所有赋权弧构成的集合$E={e_{1},e_{2},…,e_{p}}$从而为后续的静态图拓扑转换提供了基础的结构快照。层内时空切片转换的具体执行流程沿着时间轴逐层展开。在整个观察期内按时间顺序依次提取出对应于各个时间窗口$t$的网络切片$G_{t}$。针对每一个独立提取的网络切片$G_{t}$分别在其内部独立应用既定的基础图转换规则将该时间层内的节点与赋权弧直接映射为线图拓扑。各个时间窗口的切片转换过程相互独立保证了每一层线图都能准确反映原网络在该特定时段的边缘交互状态。遍历所有时间层并完成映射后将原始的离散时间层网络序列整合生成对应时刻的时序线图序列$L(G)_{T}={L(G_{1}),L(G_{2}),…,L(G_{t})}$从而实现原网络动态特征的完整过渡与保留。
022: 纵向身份耦合机制：为捕捉边在时域上的动态演化，需建立线图切片间的层间连边。若原网络中的某条边$e_{uv}$在$t$时刻和$t+1$时刻均保持活跃，则在线图中其对应的节点$v_{uv}^{L,t}$与$v_{uv}^{L,t+1}$之间建立双向耦合连边，如图1. 1所示。
023: 图1. 1 纵向身份耦合机制示意图
024: 层间耦合权重函数：层间连边的权重$C(t,t+1)$可依据该边在相邻时刻的拓扑相似度或信息熵平稳性进行赋值，为构建时序超邻接矩阵列（ASAM）确立了跨层状态转移矩阵。
025: 第二部分：线图时序超邻接矩阵构造与边重要性辨识
026: 在完成有向加权网络的线图转换后，原网络边的重要性评估问题，严格等价于线图$LG$中节点的重要性序结构辨识问题。我们将提取线图节点的多维属性，构建层内综合重要性矩阵与层间耦合矩阵，最终通过计算超邻接矩阵的特征向量实现边重要性的排序。
027: 2.1线图层内节点（原边）多属性特征提取
028: 在线图$LG_{t}$中，节点$v_{i}^{L}$间的有向连边权重$W_{ij}^{L}(t)$已经由转移算子（如乘法、调和或几何映射）求得。我们需要从局部和全局维度提取特征：
029: 步骤1：构建线图转移能力矩阵
030: 在真实复杂系统的信息级联与流量传递中，网络链路承载着动态的流转与渗透功能，而传统边权重的静态全局聚合量化方式极易掩盖局部微观交互的方向非对称性与价值倾向性。为精准刻画信息的多跳流转行为，本文将收信与送信概念拓展至线图维度，以精确度量边的综合渗透能力。在线图中，原网络的有向边被映射为节点，线图的连边则代表原链路间的流量承接关系。由于现实网络里链路汇聚上游流量与向下游分发流量的能力往往不对等，分别定义线图的收信弧与送信弧能够真实还原信息在非对称时序网络中的定向渗透轨迹。分离计算收信与送信贡献系数有效规避了单一总权值聚合带来的表征片面性，防止局部“强收弱发”或“弱收强发”的链路被误判为核心全局骨干。为了综合衡量线图节点间的上下游双向信息交互与耦合渗透能力，即衡量信息或流量流经某条边（线图节点）后，继续向下一级边渗透的能力，定义的收信弧与送信弧贡献系数：
031: 线图收信弧贡献系数：
032: $p_{i→j}^{L,in}(t)=((W_{i→j}^{L}(t))/(∑_{b∈Γ_{j}^{'}(t)} W_{b→j}^{L}(t)))#(2-1)$
033: 其中收信弧贡献系数$p_{i→j}^{L,in}(t)$衡量这条边在终点节点$j$处的话语权
034: 线图送信弧贡献系数：
035: $p_{j→i}^{L,out}(t)=((W_{j→i}^{L}(t))/(∑_{k∈Γ_{j}^{''}(t)} W_{j→k}^{L}(t)))#(2-2)$
036: 结合出入度信息，构建线图节点的转移能力矩阵：
037: $TCM^{L}(i,j,t)={p_{i→j}^{L,in}(t)+p_{j→i}^{L,out}(t),i≠j ; 0,i=j#(2-3)$
038: 步骤2：构建线图弧聚类系数矩阵
039: 聚类系数反映了连边在局部网络结构中的团簇化程度。在线图中，这代表了原网络中各连边形成紧密流转闭环的倾向。构建如下：
040: $C^{L}(i,j,t)={(((W_{i→j}^{L}(t))^{((1)/(3))}∑_{k} (W_{j→k}^{L}(t)×W_{k→i}^{L}(t))^{((1)/(3))})/(n_{i}^{L}(t)×m_{j}^{L}(t))),i≠j ; 0,其他#(2-4)$
041: 其中，$n_{i}^{L}(t)$和$m_{j}^{L}(t)$分别为线图节点$v_{i}^{L}$的入度与出度（即原网络边所承接和指向的其他边的数量）。
042: 步骤3：构建线图K-shell邻域矩阵
043: 为了刻画边在网络全局的核心-边缘位置，运用改进的K-shell分解法于线图中。计算线图节点的度$k_{i}^{L}(t)$与K-shell层级$ks_{i}^{L}(t)$，定义线图K-shell重要度：
044: $w_{ij}^{L}(t)=[k_{i}^{L}(t)+ks_{i}^{L}(t)×Q^{L}(t)]×[k_{j}^{L}(t)+ks_{j}^{L}(t)×sQ^{L}(t)]#(2-5)$
045: 结合基于共同邻域的Jaccard相似度$s_{ij}^{L}(t)$，构建矩阵：
046: $SK^{L}(i,j,t)={w_{ij}^{L}(t)×s_{ij}^{L}(t),i≠j且s_{ij}^{L}(t)≠0 ; 0,其他#(2-6)$
047: 2.2线图层内综合重要性矩阵的融合
048: 线图中不同的动力学特征在不同时刻的显著性不同。引入变异系数法计算各项指标的敏感度权重$ω_{1}(t),ω_{2}(t),ω_{3}(t)$，将单一属性赋权融合，得到线图多重高阶综合重要性矩阵：
049: $TSC^{L}(i,j,t)=ω_{1}(t)TCM^{L}(i,j,t)+ω_{2}(t)SK^{L}(i,j,t)+ω_{3}(t)C^{L}(i,j,t)#(2-7)$
050: 2.3动态时序线图的层间耦合权重测度
051: 原网络中同一条边在相邻时间层的状态可能发生变化。我们需要捕捉这种时域演化（即线图节点$v_{i}^{L,t}$与$v_{i}^{L,t+1}$之间的联系）。
052: 步骤1：线图节点传输能力系数
053: 量化线图节点（边）在单一时间切片中的整体信息吞吐能力：
054: $c_{i}^{L}(t)=((α∑_{k} W_{k→i}^{L}(t)+(1-α)∑_{j} W_{i→j}^{L}(t))/(M^{L}(t)k_{max}^{L}(t)))#(2-8)$
055: 其中$α∈(0,1)$为调节参数，$M^{L}(t)$为线图最大边权。
056: 步骤2：线图相邻与跨层传输相似度
057: 定义线图节点在时间域上的演化平稳性（即层间连边权重$o_{i}^{L}$）：
058: 相邻层：
059: $o_{i}^{L(t,t+1)}=sqrt((c_{i}^{L}(t+1)+c_{i}^{L}(t))×((1)/(2)))#(2-9)$
060: 跨层（跨越$m$层）：
061: $o_{i}^{L(t,t+m)}=((((c_{i}^{L}(t+m)+c_{i}^{L}(t+m-1))/(2))+…+c_{i}^{L}(t))×((1)/(m)))^{((1)/(m+1))}#(2-10)$
062: 2.4线图超邻接矩阵列系统模型构建
063: 2.4.1线图超邻接矩阵列系统模型
064: 基于上述得到的线图层内综合结构$TSC^{L}$与层间耦合系数$O^{L(t,t+m)}=diag(o_{1}^{L},o_{2}^{L},…,o_{|E|}^{L})$，利用矩阵分块技术构建线图时序超邻接矩阵$A^{L}$：
065: $A^{L}=[[[A^{L(1)}, TSC^{L(1)}, O^{L(1,2)}, 0, O^{L(1,3)}, 0, …]; [TSC^{L(1)'}, A^{L(1)}, 0, O^{L(1,2)}, 0, O^{L(1,3)}, …]; [O^{L(1,2)}, 0, A^{L(2)}, TSC^{L(2)}, O^{L(2,3)}, 0, …]; [0, O^{L(1,2)}, TSC^{L(2)'}, A^{L(2)}, 0, O^{L(2,3)}, …]; [O^{L(1,3)}, 0, O^{L(2,3)}, 0, A^{L(3)}, TSC^{L(3)}, …]; [0, O^{L(1,3)}, 0, O^{L(2,3)}, TSC^{L(3)'}, A^{L(3)}, …]; [⋮, ⋮, ⋮, ⋮, ⋮, ⋮, ⋱]]]$
066: 其中$A^{L(t)}$为对应时间层线图的无向无权邻接矩阵。该大型分块矩阵严格表征了“边”与“边”之间在同一时刻的竞争/协同关系，以及“边”在不同时刻的传承与消融演化。
067: 2.5.2线图超邻接矩阵列系统模型算法
068: 表1给出本文模型算法步骤，模型算法的整体计算开销主要由线图结构映射、层内多属性特征提取、层间演化耦合测度以及超大规模邻接方阵的特征向量求解四个核心维度共同构成。设原始有向加权时序网络的节点总数为$N$，随时间演化的赋权弧（边）总数为$M$，切分的时间层总数为$T$。
069: 表1 LG-DWMASAM算法步骤
070: Table 1 Steps of the LG-DWMASAM algorithm
071: [TABLE]
算法1 LG-DWMASAM算法
输入：原始有向加权时序网络$G_{T}={G_{1},G_{2},…,G_{T}}$，原网络边集规模 |E|，时间层数 T，权重映射算子，调节参数$α$，变异系数法指标权重$ω_{1}(t),ω_{2}(t),ω_{3}(t)$。 输出：线图时序超邻接矩阵$A_{L}$，以及原网络第$i$条边在第$t$个时间层的综合重要性得分$w_{it}^{L}$。 步骤 1：建立高维稀疏空矩阵$A_{L}$。依据元素身份转换与有向连边逻辑规则，将各时间层的原网络$G_{t}$映射为线图$LG_{t}$，提取各时间层线图的无向无权子邻接矩阵列$A_{L}(1),A_{L}(2),…,A_{L}(T)$并分别插入到矩阵$A_{L}$的主对角线上； 步骤 2：针对$t=1→T$，独立应用传播概率、资源瓶颈或平滑均衡算子求解线图连边权重$W_{ij}^{L}(t)$。随后计算每一时间层内的线图转移能力矩阵$TCM_{L}(t)$、弧聚类系数矩阵$C_{L}(t)$与线图K-shell邻域矩阵$SK_{L}(t)$； 步骤 3：建立空矩阵列$TSC_{L}(t)$，利用变异系数法权重融合上述三项指标计算得到层内综合重要性矩阵$TSC_{L}(t)$； 步骤 4：针对时间域演化，设定$i=1→T$且$j=i+1→T$。若$j=i+1$，则判定为相邻层状态转移，转至步骤5与步骤7执行；若$j≠i+1$，则判定为跨层状态转移，转至步骤6与步骤7执行； 步骤 5：依据线图节点传输能力系数$c_{i}^{L}(t)$计算不同时间层的相邻层传输能力相似度$o_{k}^{L}(i,j)$，其中$k=1,2,…,|E|$； 步骤 6：计算不同时间层的跨层传输能力相似度$o_{k}^{L}(i,j)$，其中$k=1,2,…,|E|$，跨越层数为$m=j-i$； 步骤 7：构建以$o_{k}^{L}(i,j)$为主对角线元素的层间相似度对角子矩阵列$O_{L}(i,j)$； 步骤 8：按照矩阵分块系统模型，将对应的$O_{L}(i,j)、TSC_{L}(i)$及其转置矩阵插入到巨型方阵$A_{L}$的对应非对角块位置以完成矩阵构建。利用Gershgorin圆盘定理求取该实对称方阵最大特征值对应的特征向量$v_{L}$，最终解算出边的综合重要性得分$w_{it}^{L}$。
072: 在线图转换阶段，原图的边被严格映射为线图节点，因此线图的单层节点规模为$M$。在层内多属性特征提取阶段，针对每一个时间切片分别计算转移能力矩阵、弧聚类系数与K-shell邻域度，单时间层的极限运算复杂度约为$O(M^{2})$，遍历所有$T$个时间层所需的时间开销为$O(T⋅M^{2})$。在动态时序层间耦合阶段，需要逐层构建相邻及跨层节点的传输能力相似度并生成对角方阵，这部分的矩阵装配时间复杂度为$O(T^{2}⋅M)$。构建完成的线图时序超邻接矩阵$A_{L}$为一个维度高达$2MT×2MT$的实对称分块方阵。运用幂法或Lanczos迭代算法求解该巨型方阵的最大特征值及特征向量时，单次矩阵向量乘法的运算量与矩阵中的非零元素个数成正比，整体求解时间复杂度记为$O(k⋅(MT)^{2})$，其中$k$表征为确保结果收敛所需的迭代逼近次数。综合上述多阶段运算过程，本研究提出的线图多维属性超邻接矩阵算法的理论上限时间复杂度为$O(kM^{2}T^{2})$。
073: 考虑到在多数真实的物理或数字演化系统观测任务中，所设定的时间窗口切片数量$T$通常为有限且远小于网络庞大连边规模$M$的常量，因此在实际工程部署与应用场景中，该辨识算法的时间复杂度可近似降维退化为$O(M^{2})$。本模型在有效避免局部维度灾难的同时，具备应对大规模复杂网络边缘序结构动力学演化特征挖掘的卓越计算可行性与工程推广价值。
074: 2.5边重要性序结构求解
075: 依据Gershgorin圆盘定理，对构建的线图时序超邻接矩阵$A_{L}$进行特征值估计。由于该矩阵为维度为$2|E|T×2|E|T$的实对称方阵，且其主对角线元素均为零，根据该定理可知矩阵的所有特征值均分布在特定的同心圆盘内。这一数学特性保障了矩阵的最大特征值及其对应的特征向量$v_{L}={v_{1}^{L},v_{2}^{L},…,v_{2|E|T}^{L}}^{T}$能够最大限度地提取并表征矩阵的全局结构特征。在该特征向量中，每$2|E|$个连续元素对应于一个特定时间层内所有边的中心性映射。最终，原网络中第$i$条边在第$t$个时间层的综合重要性得分$w_{it}^{L}$计算如下：
076: $w_{it}^{L}=v_{2|E|(t-1)+i}^{L}+v_{2|E|(t-1)+i+|E|}^{L}$
077: 公式中，$w_{it}^{L}$表征了第$t$个时间层网络上第$i$条边的特征向量中心性指标。该得分不仅能够实现对全时域范围内边重要性的精确排序，还可以动态刻画目标边在网络演化过程中重要性的演变轨迹。通过对得分矩阵$W_{L}$的分析，可以识别出在信息传播或资源流转中起关键支撑作用的核心骨干链路，为有向加权时序网络的性能优化与风险控制提供量化支撑。
078: 第三部分：模型检验方法
079: 3.1 边重要性排序单调性分析
080: $在评估网络边缘序结构时，算法的区分度是衡量其性能的核心指标之一。为量化本文模型对核心链路的精细化识别能力，研究采用排名单调性系数对重要性序列的聚集程度进行深入检验。该指标的物理意义在于评估算法能否有效打破排序中的并列现象，确保每一条承载不同局部微观交互特征的连边都能获得独立的绝对排序。当序列中共享相同重要性得分的连边数量越少时，说明算法对不同链路局部强收弱发或多维属性差异的捕捉越敏锐，整体辨识分辨率越高。这种对链路核心程度的绝对区分，为后续实施精准的流量引导或物理阻断提供了可靠的拓扑量化依据，证明了多维属性映射机制在消除维度灾难与权重冗余方面的优越性。$
081: 用于量化算法对连边重要性排序的绝对区分度。公式如下：
082: $M(R)=1-((∑_{r∈R} m_{r}(m_{r}-1))/(M(M-1)))#(3−1)$
083: 其中：$M(R)$是排名单调性系数，取值范围为$[0,1]$。值越接近$1$，说明算法的分辨率越高。$R$是由评估模型得出的核心链路排序序列。$M$为原网络中参与演化的活跃连边（即线图节点）的总数。$m_{r}$是在排序序列$R$中，具有相同重要性得分（即并列第$r$名）的连边数量。
084: 3.2 时序最大强连通分量与边删除检验分析
085: 时序最大强连通分量与边删除检验分析针对有向加权时序网络连通效用的测度，传统的节点强连通概念难以直接刻画链路失效对全局渗透路径的物理阻断效应。因此，本文基于边的主体地位重构了时序最大强连通分量，以此量化网络中由活跃连边构成的最大连通簇在全局网络运行中的主导地位。定义弧有向加权时序网络的最大强连通分量如下：
086: $LCC_{DW}^{T}=((max_{1≤i≤k(t)}(()|C_{i}^{t}|w_{i}^{t}))/(Nw_{t}))#(3−2)$
087: 公式中$LCC_{DW}^{T}$代表网络在特定时间层的最大强连通效用。变量$|C_{i}^{t}|$代表在第$t$个时间层内第$i$个强连通子图的拓扑规模（涵盖的节点数），变量$w_{i}^{t}$代表该强连通子图内所有赋权连边的权重总和（即该局部团簇的核心流量承载量）。变量$k(t)$代表当前时间层内强连通子图的总数。分母中的$N$和$w_{t}$分别代表网络的全局节点总数与所有活跃连边的全局权重总和。该指标通过局部最强连通簇的权重密度与全局状态的比值，精确度量了网络整体的连通效用水平。
088: 在建立上述基准测度后，本研究在具体的检验阶段引入边删除策略，通过模拟网络面临蓄意攻击或级联失效的场景，优先移除由各模型评估出的高重要性链路集合。通过对比核心骨干链路被剔除前后网络全局连通效用的下降幅度，可以直观且客观地验证评估结果的准确性。
089: $V=((LCC_{DW}^{T}(G)-LCC_{DW}^{T}(G\E_{σ}))/(LCC_{DW}^{T}(G)))#(3−3)$
090: 公式中$V$代表网络连通效用下降率，该数值越大，证明被移除的连边集合对维持全局流通的咽喉作用越显著，侧面印证了算法排序的精准性。变量$LCC_{DW}^{T}(G)$代表初始未遭受攻击时网络的最大时序强连通效用值。变量$E_{σ}$代表按照本文模型辨识出的重要性排名前$σ$比例的核心链路集合。变量$LCC_{DW}^{T}(G\E_{σ})$代表从原网络中精确阻断链路集合$E_{σ}$后，残存网络的最大时序强连通效用值。
091: 3.3 链路级联传播能力分析
092: 核心链路的真实价值不仅体现在静态拓扑的支撑上，更突显于对动态流量渗透和信息级联扩散的驱动能力。在完成网络到线图的无损拓扑转换后，本研究将传统的线性阈值传播动力学模型拓展至线图节点即原网络链路维度进行演化分析。在该机制下，原图中的有向连边被赋予特定的激活阈值以表征其被上游流量渗透的阻力大小，传播过程彻底演化为线图空间内的级联激活现象。当某一未激活链路接收到来自上游已激活链路群的综合渗透强度超越其固有阈值时，该链路即被触发并继续向下游导流。以不同算法识别出的关键连边集合作为初始扩散种子，深入对比其在网络中触发的最终级联规模与渗透深度，能够最为真实地反映出所辨识骨干链路在现实复杂系统中实际的导流效能与风险扩散潜力。
093: $∑_{i∈S(k)∩Γ_{j}^{L,in}} p_{i→j}^{L,in}(t)≥θ_{j}#(3−4)$
094: 在该触发条件公式中，$S(k)$代表在第$k$次级联传播后已经处于激活状态的链路集合，也就是已被激活的线图节点集合。变量$Γ_{j}^{L,in}$代表在拓扑上直接指向链路$j$的所有上游邻接链路集合，即线图中的入度邻居。变量$p_{i→j}^{L,in}(t)$代表在第$t$个时间层中上游链路$i$对下游链路$j$的定向渗透强度，这与前文定义的线图收信弧贡献系数相对应。变量$θ_{j}$代表链路$j$固有的激活阈值，该阈值表征了该链路被上游流量贯通并产生实质性导流的物理阻力或容量门槛。
095: 第四部分 模型实证数据分析
096: 参考文献：（最后汇总，目前引用文献都放在每部分下）
097: [1]胡钢,康凯,胡俊杰,等. 基于弧有向加权时序网络超邻接矩阵列的节点重要性序结构演化辨识[J].系统工程与电子技术,2026,48(03):970-985.